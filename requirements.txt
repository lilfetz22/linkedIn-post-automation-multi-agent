# =============================================================================
# LinkedIn Post Automation Multi-Agent System - Dependencies
# =============================================================================
# Install with: pip install -r requirements.txt
# For development: pip install -r requirements.txt
# For fresh install test: pip install --no-cache-dir -r requirements.txt
# =============================================================================

# -----------------------------------------------------------------------------
# Core LLM Dependencies (Google Gemini)
# -----------------------------------------------------------------------------
google-generativeai>=0.8.0,<1.0.0   # Gemini API client (text + image generation)
google-genai>=1.50.0,<2.0.0         # New Gemini client with grounding support
python-dotenv>=1.0.0,<2.0.0         # Environment variable loading from .env

# -----------------------------------------------------------------------------
# RAG and Vector Embeddings (ChromaDB)
# -----------------------------------------------------------------------------
chromadb>=1.0.0,<2.0.0              # Vector store for memory bank RAG queries

# -----------------------------------------------------------------------------
# Data Validation
# -----------------------------------------------------------------------------
pydantic>=2.0.0,<3.0.0              # Envelope validation and data models

# -----------------------------------------------------------------------------
# Grammar and Spell Checking
# -----------------------------------------------------------------------------
language-tool-python>=2.7.0,<4.0.0  # Local grammar checking fallback for Reviewer Agent

# -----------------------------------------------------------------------------
# Progress Indicators
# -----------------------------------------------------------------------------
tqdm>=4.66.0,<5.0.0                 # Progress bars for long-running operations

# -----------------------------------------------------------------------------
# Testing Framework
# -----------------------------------------------------------------------------
pytest>=8.0.0,<10.0.0               # Test runner
pytest-cov>=5.0.0,<8.0.0            # Coverage plugin for pytest
coverage>=7.0.0,<8.0.0              # Code coverage measurement

# -----------------------------------------------------------------------------
# Code Quality and Linting
# -----------------------------------------------------------------------------
flake8>=7.0.0,<8.0.0                # Python linter
flake8-bugbear>=24.0.0              # Additional flake8 checks for common bugs

# -----------------------------------------------------------------------------
# Implicit Dependencies (installed automatically, listed for reference)
# -----------------------------------------------------------------------------
# These are pulled in by the above packages:
# - grpcio, grpcio-status (for Google API communication)
# - protobuf, proto-plus (for protocol buffers)
# - numpy (for ChromaDB)
# - requests (for HTTP calls)
# - typing_extensions (for type hints)
# - onnxruntime (for ChromaDB embeddings)
# - httpx (for async HTTP)
